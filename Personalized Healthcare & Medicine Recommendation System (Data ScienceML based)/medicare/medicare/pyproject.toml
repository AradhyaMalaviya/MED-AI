[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
    "venv",
    ".venv",
    "env",
    "__pycache__",
    ".pytest_cache",
    "htmlcov",
    "build",
    "dist",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
